var x = 5;
var y = 5;

function setup() {
	createCanvas(500, 500);
}

function draw() {
	background(220);
	ellipse(x, y, 10, 10);

	for ( x = 5 ; x < 500; x = x + 10) {

		ellipse(x, height/2, 10, 10);
	}

}
